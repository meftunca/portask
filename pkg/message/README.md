# message/ (Message Structure)

This package is reserved for message structure types. Most message types are currently defined under `types/`. This directory is currently empty or minimal.

## TODO / Missing Features
- [ ] Move or expand message types here if needed
- [ ] Add message validation helpers

---

For details, see the code and comments in each file.
